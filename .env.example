# ROCm Environment Variables for AMD Radeon RX 7900 GRE
# Copy this to .env or export these variables before running

# GPU architecture override (gfx1100 for RX 7900 GRE)
HSA_OVERRIDE_GFX_VERSION=11.0.0

# ROCm installation path
ROCM_PATH=/opt/rocm

# GPU selection (0 for first GPU)
HIP_VISIBLE_DEVICES=0

# PyTorch memory management
PYTORCH_HIP_ALLOC_CONF=garbage_collection_threshold:0.6,max_split_size_mb:128

# Hugging Face cache location (optional)
# HF_HOME=/home/jare16/.cache/huggingface
# TRANSFORMERS_CACHE=/home/jare16/.cache/huggingface/transformers

# Database path (optional, for consistency with Docker)
# DATABASE_PATH=/home/jare16/LLM/data/models.db
