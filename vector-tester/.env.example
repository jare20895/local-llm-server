# Base URL of the FastAPI server (llm-server service inside docker-compose)
LLM_API_BASE=http://llm-server:8000

# Path for the tester SQLite DB (inside the container)
LOG_DB_PATH=/app/data/vector-tester.db
